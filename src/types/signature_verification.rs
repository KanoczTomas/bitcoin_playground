#[derive(Debug, PartialEq)]
/// Type to represent signature verification.
pub enum SignatureVerification {
    /// When signature verification succeeds
    Successful,
    /// When signature verification fails11
    Failed
}
